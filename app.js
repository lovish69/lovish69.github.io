(()=>{
const dialog=document.getElementById('project-dialog');
window.openProject=function(index){const p=projects[index];if(!p)return;const d=p.detail;document.getElementById('brief-content').innerHTML=`<span class="eyebrow">${p.label}</span><h2 id="brief-title">${p.title}</h2><p>${d.focus}</p><div class="project-tags">${p.tags.map(t=>`<span>${t}</span>`).join('')}</div><div class="brief-grid"><div class="brief-block"><h3>IMPLEMENTATION</h3><ul>${d.steps.map(t=>`<li>${t}</li>`).join('')}</ul></div><div class="brief-block"><h3>ENGINEERING FOCUS</h3><p>${d.learn}</p><h3 style="margin-top:22px">EXPLORE THE EVIDENCE</h3><p>The repository contains implementation files and project documentation. Start with the README for setup and architecture.</p></div></div><p class="brief-note">${d.scope}</p><a class="button" href="https://github.com/lovish69/${p.repo}" target="_blank" rel="noopener noreferrer">Open repository <span>↗</span></a>`;dialog.showModal();dialog.scrollTop=0;document.body.classList.add('modal-open')};
document.querySelectorAll('[data-case]').forEach(b=>b.addEventListener('click',()=>window.openProject(+b.dataset.case)));
document.querySelectorAll('[data-close]').forEach(b=>b.addEventListener('click',()=>document.getElementById(b.dataset.close).close()));document.querySelectorAll('dialog').forEach(d=>{d.addEventListener('close',()=>document.body.classList.remove('modal-open'));d.addEventListener('click',e=>{if(e.target===d){const r=d.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)d.close()}})});
document.querySelectorAll('.filter').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(x=>{x.classList.toggle('active',x===b);x.setAttribute('aria-pressed',String(x===b))});let count=0;document.querySelectorAll('.project-card').forEach(c=>{c.hidden=b.dataset.filter!=='all'&&!c.dataset.category.split(' ').includes(b.dataset.filter);if(!c.hidden)count++});document.getElementById('project-count').textContent=String(count).padStart(2,'0')+' / 07 ON DISPLAY'}));
const steps=[{a:'Terraform',b:'AWS / EKS',title:'Infrastructure starts in version control.',text:'Define EC2, networking, and EKS resources in Terraform. Connect provisioning steps to Jenkins and keep the configuration inspectable.',repo:'jenkins-terraform-k8s'},{a:'Jenkins',b:'Scan / build',title:'Every change deserves a few good checks.',text:'Build container images, run code-quality analysis, check dependencies, and scan images before delivery. The DevSecOps project explores how those steps fit together.',repo:'DevSecOps-Zomato'},{a:'Argo CD',b:'Kubernetes',title:'Let Git describe where the application goes.',text:'Package application configuration with Helm and use Argo CD to synchronize Kubernetes deployments from version-controlled definitions.',repo:'DevOpsified-Complete-project'},{a:'Prometheus',b:'Grafana',title:'Make running systems easier to understand.',text:'Collect Kubernetes metrics, query CPU and memory usage with PromQL, and explore resource behavior through Grafana dashboards.',repo:'observability-project'}];
const tabs=[...document.querySelectorAll('[data-step]')],panel=document.getElementById('workflow-panel');function step(index){const stationNames=['Infrastructure','Build & security','Kubernetes','Observability'];const stationDescriptions=['Terraform defines the foundation. AWS provides the building blocks.','Package changes, check dependencies, and inspect container images.','Declare workload configuration and synchronize it through GitOps.','Collect metrics and turn resource behavior into visible signals.'];document.getElementById('lab-name').textContent=stationNames[index];document.getElementById('lab-description').textContent=stationDescriptions[index];document.querySelectorAll('[data-lab]').forEach(b=>{b.classList.toggle('active',+b.dataset.lab===index);b.setAttribute('aria-pressed',String(+b.dataset.lab===index))});window.dispatchEvent(new CustomEvent('platform-stage',{detail:index}));tabs.forEach((b,i)=>{b.classList.toggle('active',i===index);b.setAttribute('aria-selected',String(i===index));b.tabIndex=i===index?0:-1});const d=steps[index];panel.setAttribute('aria-labelledby','step-'+index);panel.innerHTML=`<div class="workflow-visual" aria-hidden="true"><div class="work-block">${d.a}</div><span>→</span><div class="work-block">${d.b}</div></div><div><h3>${d.title}</h3><p>${d.text}</p></div><a href="https://github.com/lovish69/${d.repo}" target="_blank" rel="noopener noreferrer">Explore the implementation ↗</a>`}window.selectWorkflow=step;document.querySelectorAll('[data-lab]').forEach(b=>b.addEventListener('click',()=>step(+b.dataset.lab)));tabs.forEach((b,i)=>{b.addEventListener('click',()=>step(i));b.addEventListener('keydown',e=>{let n;if(e.key==='ArrowDown'||e.key==='ArrowRight')n=(i+1)%4;if(e.key==='ArrowUp'||e.key==='ArrowLeft')n=(i+3)%4;if(e.key==='Home')n=0;if(e.key==='End')n=3;if(n!==undefined){e.preventDefault();step(n);tabs[n].focus()}})});const mq=matchMedia('(max-width:800px)');function orient(){document.querySelector('[role=tablist]').setAttribute('aria-orientation',mq.matches?'horizontal':'vertical')}mq.addEventListener('change',orient);orient();
const command=document.getElementById('command-dialog'),search=document.getElementById('command-search'),list=document.getElementById('command-list');const destinations=[['Selected projects','work'],['Delivery lifecycle','workflow'],['About & toolkit','about'],['Education & experience','journey'],['Certifications & Credly','credentials'],['Get in touch','contact']];list.innerHTML=destinations.map(([name,id])=>`<a href="#${id}">${name}<span>↗</span></a>`).join('');function filter(){let n=0;list.querySelectorAll('a').forEach(a=>{a.hidden=!a.textContent.toLowerCase().includes(search.value.toLowerCase());if(!a.hidden)n++});document.querySelector('.command-empty').hidden=n>0}function showCommand(){if(dialog.open)return;command.showModal();document.body.classList.add('modal-open');search.value='';filter();search.focus()}document.getElementById('open-command').addEventListener('click',showCommand);document.addEventListener('keydown',e=>{if(e.key==='Escape'&&command.open){e.preventDefault();command.close()}if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();if(command.open)command.close();else showCommand()}});search.addEventListener('input',filter);search.addEventListener('keydown',e=>{if(e.key==='ArrowDown'){e.preventDefault();list.querySelector('a:not([hidden])')?.focus()}if(e.key==='Enter')list.querySelector('a:not([hidden])')?.click()});list.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>command.close()));
document.getElementById('copy-email').addEventListener('click',async()=>{try{await navigator.clipboard.writeText('lovishbarber69@gmail.com');document.getElementById('copy-status').textContent='Email address copied.'}catch{document.getElementById('copy-status').textContent='Email: lovishbarber69@gmail.com'}});document.getElementById('year').textContent=new Date().getFullYear();
let waiting=false;function progress(){const range=document.documentElement.scrollHeight-innerHeight;document.getElementById('progress').style.width=(range?scrollY/range*100:0)+'%';waiting=false}addEventListener('scroll',()=>{if(!waiting){requestAnimationFrame(progress);waiting=true}},{passive:true});progress();const observer=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting)document.querySelectorAll('.nav-links a[href^="#"]').forEach(a=>{if(a.hash==='#'+e.target.id)a.setAttribute('aria-current','location');else a.removeAttribute('aria-current')})})},{rootMargin:'-15% 0px -60% 0px'});document.querySelectorAll('main section[id]').forEach(s=>observer.observe(s));
const depthReduce=matchMedia('(prefers-reduced-motion: reduce)');if(matchMedia('(hover:hover) and (pointer:fine)').matches){document.querySelectorAll('.project-card,.credential,[data-depth-card]').forEach(card=>{card.addEventListener('pointermove',e=>{if(depthReduce.matches)return;const r=card.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.setProperty('--tilt-x',`${-y*7}deg`);card.style.setProperty('--tilt-y',`${x*9}deg`);card.style.setProperty('--shine-x',`${(x+.5)*100}%`);card.style.setProperty('--shine-y',`${(y+.5)*100}%`);card.classList.add('is-tilted')});card.addEventListener('pointerleave',()=>{card.classList.remove('is-tilted');card.style.setProperty('--tilt-x','0deg');card.style.setProperty('--tilt-y','0deg')})});depthReduce.addEventListener('change',()=>{if(depthReduce.matches)document.querySelectorAll('.is-tilted').forEach(c=>c.classList.remove('is-tilted'))})}
})();
